# CodeAlpha ML Internship — Requirements
# Install with: pip install -r requirements.txt

# Core
numpy>=1.24
pandas>=1.5
matplotlib>=3.6
seaborn>=0.12

# Machine Learning
scikit-learn>=1.2

# Deep Learning (Task 1)
tensorflow>=2.12       # or tensorflow-cpu>=2.12 for CPU-only

# Optional: XGBoost (Task 2)
xgboost>=1.7

# Optional: EMNIST letters dataset (Task 1 extension)
emnist>=0.0

# Image loading (for single-image inference demo)
Pillow>=9.0
